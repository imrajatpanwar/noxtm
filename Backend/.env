# Server Configuration
PORT=5000

# MongoDB Configuration - Using MongoDB Atlas
MONGODB_URI=mongodb+srv://noxtmstudio:qWWniMmKtOxnJcm9@cluster0.4jneyth.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# For local development, you can use local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/react-app

# For production, use a strong random JWT secret
# JWT_SECRET=your-very-long-random-secret-key-here

# Email Configuration (for noreply@noxtm.com)
# Using your own Postfix mail server on 185.137.122.61

# ACTIVE: Own Mail Server (Postfix on your VPS)
EMAIL_HOST=185.137.122.61
EMAIL_PORT=25
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@noxtm.com

# BACKUP OPTIONS (if you want to use external services):
# OPTION 1: Zoho Mail
# EMAIL_HOST=smtp.zoho.com
# EMAIL_PORT=587
# EMAIL_USER=noreply@noxtm.com
# EMAIL_PASS=YOUR_ZOHO_PASSWORD_HERE
# EMAIL_FROM=noreply@noxtm.com

# OPTION 2: Mailgun (5000 emails/month FREE)
# Sign up at: https://www.mailgun.com
# 1. Add and verify noxtm.com domain
# 2. Update DNS records (TXT records for verification)
# 3. Get SMTP credentials from: Sending > Domain settings > SMTP
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USER=postmaster@mg.noxtm.com
# EMAIL_PASS=your-mailgun-smtp-password
# EMAIL_FROM=noreply@noxtm.com

# OPTION 3: Direct Server SMTP (Advanced - Requires mail server setup on 185.137.122.61)
# NOT RECOMMENDED: Requires Postfix/Exim setup, SPF/DKIM/DMARC, proper reverse DNS
# Most ISPs block port 25, and emails often go to spam without proper setup
# EMAIL_HOST=localhost
# EMAIL_PORT=25
# EMAIL_USER=
# EMAIL_PASS=
# EMAIL_FROM=noreply@noxtm.com

# OPTION 4: Cloudflare Email Workers (Free, but requires Workers setup)
# Use Cloudflare Email Workers with MailChannels (no SMTP needed)
# See: https://developers.cloudflare.com/email-routing/email-workers/
# Requires code changes to use HTTP API instead of SMTP
