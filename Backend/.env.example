# Server Configuration
PORT=5000

# MongoDB Configuration - Using MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (for noreply@noxtm.com)
# Option 1: Using a professional email service (Recommended)
# For custom domain email, you can use:
# - Google Workspace (smtp.gmail.com)
# - Zoho Mail (smtp.zoho.com)
# - SendGrid (smtp.sendgrid.net)
# - AWS SES
# - Mailgun

# If using Google Workspace with your custom domain:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=noreply@yourdomain.com
EMAIL_PASS=your-app-specific-password-here
EMAIL_FROM=noreply@yourdomain.com

# If using SendGrid (recommended for transactional emails):
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASS=your-sendgrid-api-key
# EMAIL_FROM=noreply@yourdomain.com
